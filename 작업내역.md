# 재현테크 장비 견적 요청 프로그램 작업내역

**파일명:** 장비 견적 요청 프로그램.html  
**작업일:** 2026-05-06  
**작업자:** Claude Sonnet 4.6

---

## 1차 작업 — UI/UX 개선 및 표 간격 수정

### CSS 수정
- `mini-table` 첫 번째 컬럼 고정 너비(`160px`), `white-space:nowrap`, `font-weight:600` 적용 → 구분/내용 컬럼 정렬 개선
- 셀 패딩 `9px` → `11px 16px`, `vertical-align:top`, `word-break:keep-all` 추가
- 짝수 행 배경색(`#fafcff`) 추가 → 줄 구분 가시성 향상
- `.rev { display:none }` 기본값 추가 → 매출 입력 필드 초기 깜빡임 방지
- 체크박스 `.check input[type=checkbox]`에 `flex-shrink:0`, 고정 크기(`16×16`) 추가 → 긴 텍스트 줄 바꿈 시 체크박스 위치 고정
- `.check` 호버 시 배경·테두리 색상 변화 추가
- 버튼(`.btn`) 호버 효과 추가

### 매출 그리드 수정
- `id="revGrid"` 추가
- 사업자등록일에 따라 **2컬럼 / 3컬럼** 동적 전환 → 3컬럼에 2개만 표시될 때의 빈 공간 제거

### UX 개선 (JS)
- `validateStep()` 함수 추가
  - 2단계: 개인정보 동의 미체크 또는 필수 항목(업체명·사업자번호·연락처·이메일) 미입력 시 다음 단계 진행 차단
  - 3단계: 문제 공정·도입 목적 미선택 시 차단
- 왼쪽 탭으로 앞 단계 건너뛰기도 동일하게 검증
- 견적 요청 시 최종 확인사항 4개 미체크하면 다운로드 차단
- `requestQuote()` 개선: 필수 정보 미입력 체크 후 다운로드

---

## 2차 작업 — 사업 안내 콘텐츠 수정

### 헤더 변경
- 배지: `재현테크` → `(주)재현테크`
- 헤더 설명 두 번째 문장 줄바꿈 후 "본 페이지는 공정 분석 및 해결을 위한 장비 도입 견적을 검토하기 위한 것입니다."로 변경

### 0단계 사업 안내 섹션
- 섹션 내 모든 "재현테크" → "(주)재현테크" 변경 (섹션 설명, 박스 제목, 박스 본문)
- 재현테크 안내 박스: "단, 공급기업 등록은 품질·선정·지원금 지급을 보증하는 의미가 아니며…" 문장 삭제

### 사업 핵심 요약 표 수정
- **자부담 행 내용 확장:**
  - 부가세 별도 안내 추가: 총 사업비 4,800만원의 10% = 480만원
  - 실질 현금 준비 필요액: 자부담 현금 1,200만원 + 부가세 480만원 = **총 1,680만원** (빨간 강조)
- **사업기간 행 신규 추가** (협약기간 위): 협약일 ~ 2026년 12월 20일

### 공급기업 역할 제한 → 사업 신청 절차 전면 교체
- ① 신청기한: 5월 18일(월) ~ 6월 2일(화) 18:00 / 소상공인24 직접 신청
- ② 준비사항 7개 항목 (동영상 사업계획서, 비교견적서, 부가가치세 신고서, 견적서, 하드웨어 이미지, 납세 증명서, 가점 서류)
- ③ 의무사항 2개 항목 (5년 보유·사용 의무 / 환수 조건)

---

## 3차 작업 — 개인정보 수집·이용 동의서 법적 요건 충족

**개인정보 보호법 제15조·제17조 기준 필수 기재사항 추가:**

| 항목 | 내용 |
|---|---|
| 개인정보처리자 | (주)재현테크 (담당부서: 영업팀) |
| 수집·이용 목적 | 견적 검토, 상담 연락, 기술자료 제공 |
| 수집 항목 | 필수(업체명·사업자번호·대표자명·연락처·이메일) / 선택(주소·공정현황·요청사항) |
| 보유·이용 기간 | 동의일로부터 1년, 완료 후 즉시 파기 |
| 제3자 제공 | 원칙 미제공, 법령 예외 명시 |
| 처리 위탁 | 없음 명시 |
| 정보주체 권리 | 열람·정정·삭제·처리정지 권리 안내 |
| 동의 거부권 | 거부 가능 + 불이익(서비스 제한) 고지 |

- 체크박스 **[필수]** 빨간 태그 추가 및 문구 구체화
- `privacyAgree` / `contactAgree` ID 유지 → 기존 유효성 검사와 호환

---

## 4차 작업 — 3·4·5단계 전면 개편

### 3단계 공정 문제 분석

**purposeOptions 전면 교체** (인쇄업 실제 문제점 기반)

공정별(절단·커팅 / 인쇄 / 기타) × 목적별(기존장비 교체 / 신규장비 도입) 조합으로 각 8~10개 항목, **"현재 문제 → 개선 목표"** 형식으로 작성:

- 절단/커팅 공정 - 기존장비 교체 (10개): 노후 고장·정밀도 저하·사이즈 부족·두꺼운 소재 불가·속도 저하·수동 보정·데이터 연동 불가·소모품 문제·작업환경·데이터 관리
- 절단/커팅 공정 - 신규장비 도입 (8개): 외주 납기·원가·소량 대응·공정 일원화·샘플 제작·신규 품목·긴급 대응·품질 편차
- 인쇄 공정 - 기존장비 교체 (10개): 고장·헤드막힘·속도·색상편차·특수잉크·사이즈·UV경화·잉크비용·RIP호환·AS불가
- 인쇄 공정 - 신규장비 도입 (10개): 외주 의존·다소재·소량 대응·특수인쇄·샘플 대응·신규 품목·외주비·긴급 납품·커팅 연계·대형 소재
- 기타 - 기존장비 교체 (6개) / 신규장비 도입 (5개)

**"문제 설명 및 개선 목표" 박스 삭제** (텍스트 입력 2개 + 기대효과 체크박스 8개 전체 제거)

### 4단계 도입 희망 장비 선택

**장비 목록 전면 교체:**

| 카테고리 | 장비명 |
|---|---|
| 프린터 (11종) | XTRA OR16, XTRA OR36, XTRA R20, XTRA 2512S, X20, JU2513+, JU1810+, JU9060+, JU1361, T8Q, T9M |
| 평판커팅기 (4종) | JP0806, JP1113, JP1625, SG1625 |

- **카테고리 헤더** 구분 표시 (프린터 / 평판커팅기)
- **단일 선택** 전환 — 다른 항목 클릭 시 이전 선택 자동 해제
- `recommendEquip()` 단순화: 선택 공정에 따라 추천 카테고리 안내

### 5단계 제출 내용 확인

**입력 내용 요약 가시성 개선:**
- `<pre>` 텍스트 방식 → 구조화된 HTML 카드 방식으로 전환
- 섹션별 제목(border-left 강조), 라벨/값 구분 레이아웃
- 체크 여부 ✔/✘ 컬러 배지 (`sum-chk ok` / `sum-chk no`)
- 선택한 문제·목표 태그 형태 표시
- `buildSummary()` → HTML 생성 + `buildPlainText()` 분리
- 다운로드(.txt) 는 `buildPlainText()` 사용으로 기존 기능 유지

---

## 5차 작업 — 평균매출 판정 로직 수정

**기준:** 소공인 스마트제조 지원사업 평균매출 조건은 **2억원 초과** 기업이 지원 대상

| 조건 | 변경 전 | 변경 후 |
|---|---|---|
| 평균매출 > 2억원 | 초록 "기준 충족 가능" | 초록 **"2억원 초과 — 지원 대상 가능"** |
| 평균매출 ≤ 2억원 | 빨강 "기준 초과 가능성 있음" | 빨강 **"2억원 이하 — 기준 미달 (지원 대상 제외)"** |

판정 로직: `avg <= limit` (이하면 OK) → `avg > limit` (초과해야 OK) 로 수정

---

## 향후 고려 사항 (5차 작업 시점)

- **Google Apps Script 연동**: 견적 요청 버튼 클릭 시 자동 접수 처리 (현재 .txt 다운로드만 제공)
- **개인정보처리자 정보 보완**: (주)재현테크 사업장 주소 및 개인정보 보호 책임자 연락처 추가 필요
- **장비 상세 스펙 연동**: 장비 선택 시 상세 사양(출력 사이즈, 해상도, 잉크 종류 등) 팝업 또는 표시 기능 추가 가능

---

## 6차 작업 — Google Apps Script 백엔드 연동 및 관리자 페이지 개발

**작업일:** 2026-05-07  
**주요 파일:** `Code.gs`, `admin.html`, `quote.html`

### Google Apps Script (Code.gs) 구축

- `SPREADSHEET_ID`: `1HoFkaRY0xOGEriXAjrQ7tyH9LOZ5UkPamyRzxc_W3Ts`
- **신청관리** 시트 (20컬럼): 접수번호 ~ 담당자
- **견적서발급관리** 시트 (12컬럼): 견적번호 ~ 상태

| action | 메서드 | 기능 |
|---|---|---|
| `submit` | POST | 견적 신청 접수 → 신청관리 시트 행 추가, 접수번호(REQ-) 반환 |
| `confirm` | POST | 견적 확정 → 신청관리 상태 업데이트 + 견적서발급관리 행 추가/수정 |
| `saveQuote` | POST | 견적서 PDF/HTML → Google Drive 저장 |
| `updateAssignee` | POST | 담당자 배정 → 신청관리 시트 20번 컬럼 업데이트 |
| `list` | GET | 신청관리 전체 목록 반환 (견적 데이터 조인 포함) |
| `get` | GET | 단건 조회 |
| `listQuotes` | GET | 견적서발급관리 전체 목록 반환 |

- JSONP(`callback` 파라미터) 지원으로 CORS 우회
- `generateId(prefix)`: `REQ-YYYYMMDD-NNNNN` / `QT-YYYYMMDD-NNNNN` 형식

### 관리자 페이지 (admin.html) 신규 개발

**로그인 시스템**
- localStorage 기반 사용자 계정 관리 (`jhtech_users`)
- 기본 계정: `admin`(관리자), `park`(박현석), `lee`(이담당)
- `isAdmin` 플래그로 관리자/일반 구분
- `logout()` 함수: 화면 초기화 후 로그인 화면 복귀

**신청 목록 관리**
- GAS `list` action으로 실시간 데이터 로드
- 상태별 탭 필터 (전체 / 신규 / 검토중 / 확정 / 완료)
- 카드형 목록 UI, 접수번호·업체명·장비·상태 표시

**견적서 상세 편집**
- `editMode` 조건: `isAssignedToMe && (!isConfirmed || isRevising)`
  - 담당자로 지정된 사용자만 편집 가능
  - 확정 후에는 수정(재발행) 모드 진입 시에만 편집 허용
- `syncFormState()`: 추가옵션 행 추가/삭제 전 DOM 상태 저장 (체크박스 해제 버그 방지)

**포함옵션·추가옵션 관리**
- 포함옵션 체크박스 (`.inc-opt`)
- 추가옵션 동적 행 추가/삭제 (`addExtraOpt()` / `removeExtra()`)
- `syncFormState()` → `renderDetail()` 순서로 항상 상태 보존

**가격 계산**
- `basePrice` 입력 (장비 기본가) 와 `totalDisplay` 표시 분리 → 이중 계산 버그 방지
- `recalcPrice()`: base + extraOpts 합산 → totalDisplay 갱신
- 추가옵션별 금액 breakdown 표시

**장비 변경**
- 관리자(admin) 계정 editMode에서 장비 드롭다운 표시
- `changeEquip(name)`: 장비 선택 시 장비 사진 실시간 갱신

**견적서 출력 (printQuote)**
- 담당자 이름/연락처: `r.assignee` 기준 조회 → 없으면 `currentUser` 폴백
- 잉크가격 행 제거
- `supplyPrice=0` 버그 수정: `basePrice` 입력 없을 때 기존 값 유지

**버전 관리**
- `getNextVersion(quoteNo)`: `QT-xxx` → `QT-xxx-R2` → `QT-xxx-R3` 자동 증가
- 확정 후 수정 시 새 버전 번호로 견적서 재발행

---

## 7차 작업 — 담당자 배정 기능

**작업일:** 2026-05-07

- 신청 목록에서 담당자 드롭다운 선택
- **저장 버튼** 클릭 → GAS `updateAssignee` action 호출 → 신청관리 시트 20번 컬럼 저장
- 저장 후 버튼이 **수정 버튼**으로 전환 (다시 수정 클릭 시 드롭다운 활성화)
- 담당자 조회 우선순위: 시트 데이터(`r.assignee`) → localStorage → 빈값
- 담당자 지정 시 해당 계정으로만 견적서 편집 가능 (editMode 조건 연동)

---

## 8차 작업 — GitHub Pages 배포

**작업일:** 2026-05-07  
**저장소:** https://github.com/jhtechsmart-cloud/jhtechsmart

- Git 초기화 및 원격 저장소 연결
- Git 계정: `jhtechsmart` / `jhtechsmart@gmail.com`
- `.gitignore`: `.claude/`, `견적서/법인도장.png` 제외
- 파일명 변경: `장비 견적 요청 프로그램.html` → `quote.html`, `관리자.html` → `admin.html`
- GitHub Pages 활성화 (main 브랜치 / root 경로)
- 접속 주소:
  - 견적 신청: `https://jhtechsmart-cloud.github.io/jhtechsmart/quote.html`
  - 관리자: `https://jhtechsmart-cloud.github.io/jhtechsmart/admin.html`

---

## 9차 작업 — OG 메타태그 (카카오톡 공유)

**작업일:** 2026-05-07

- `quote.html`, `admin.html` 양쪽에 OG 태그 추가
- `logo.png` (800×800 정사각형) 생성 → `재현테크 로고.png` 기반
- OG 이미지: `https://jhtechsmart-cloud.github.io/jhtechsmart/logo.png`
- 카카오톡 미리보기에서 로고 전체 표시 확인

---

## 10차 작업 — quote.html 콘텐츠 개편

**작업일:** 2026-05-08

| 항목 | 변경 전 | 변경 후 |
|---|---|---|
| 신청원칙 섹션 | 별도 섹션 존재 | 삭제 |
| 최대구입비 안내 | 없음 | "최대 구입비 4,800만원 + 부가세 480만원" 추가 |
| 유의사항 | 단락 형태 | `<ul>` 4개 항목으로 분리 |
| 신청자격 행 | 없음 | 사업핵심요약 표에 추가 (소공인 제조업 10인 미만, 2억원 이상) |
| 자가진단 ③ | "부가세 별도" 항목 존재 | 삭제, ①~⑦ 재번호 매김, 번호 폰트 16px |
| 제출 후 화면 | 없음 | 견적신청 완료 화면 (업체명·연락처·선택장비·접수일시 표시) |

---

## 11차 작업 — PDF 자동 저장 (Google Drive)

**작업일:** 2026-05-08

**변경 전:** 견적서 확정 시 HTML 파일로 Google Drive 저장  
**변경 후:** 클라이언트에서 PDF 생성 후 base64로 GAS 전송 → .pdf 파일로 저장

### admin.html 변경사항

- `loadHtml2Pdf()`: CDN에서 html2pdf.js 동적 로드 (중복 로드 방지)
- `saveQuoteToDrive(html, quoteNo, company)`:
  1. html2pdf.js로 견적서 HTML → PDF Blob 생성
  2. FileReader로 base64 인코딩
  3. GAS에 `{pdf: base64, filename, company}` POST
- 파일명 형식: `업체명_YYYYMMDD.pdf`

### Code.gs 변경사항

- `handleSaveQuote()`: `data.pdf` 존재 시 base64 디코딩 → `.pdf` 파일 저장
- 폴백: `data.html` 존재 시 `.html` 파일 저장
- 저장 위치: `재현테크_견적서/YYYY-MM/` 서브폴더
- 저장 후 링크 공유 설정 (`ANYONE_WITH_LINK` VIEW 권한)

---

## GAS 배포 이력

| 버전 | URL (suffix) | 주요 변경 |
|---|---|---|
| v1 | `AKfycbwLQHXT...` | 초기 배포 |
| v2 | `AKfycbzcl9GZ...` | PDF 저장 지원 추가 |

**현재 사용 중인 GAS URL:**  
`https://script.google.com/macros/s/AKfycbzcl9GZ--OaM7DkcxYcteRbE843Jnq75KnfipLuD7ixBBRXnOxCuQuTZB96eWSwrxhl/exec`

---

## 버그 수정 이력

| 버그 | 원인 | 수정 방법 |
|---|---|---|
| 추가옵션 추가/삭제 시 포함옵션 체크 해제 | `renderDetail()` 호출 전 DOM 상태 미저장 | `syncFormState()` 추가 후 re-render |
| 가격 이중 계산 | `supplyPrice` 입력에 합계가 덮어씌워짐 | `basePrice`(장비가) / `totalDisplay`(합계 표시) 분리 |
| 견적서 출력 시 장비 가격 0 | 확정 모드에서 `basePrice` input 없음 | `if(bpEl)` 조건 추가, 기존 값 유지 |
| 담당자가 아닌 박현석 부장으로 출력 | `printQuote()`가 항상 `currentUser` 사용 | `r.assignee` 먼저 조회 후 fallback |
| OG 이미지 깨짐 | 1200×630 이미지로 카카오톡 미리보기 잘림 | 800×800 정사각형으로 교체 |

---

## 12차 작업 — Enterprise CPQ UI 리뉴얼 + 시트 정합성 정비

**작업일:** 2026-05-11 ~ 2026-05-12
**관련 파일:** `admin.html`, `appscript/Code.gs`, `docs/issues.md`, `docs/code-analysis.md`, `docs/layout-samples/`, `logo-header.png`(신규)

### 12-A. 시트 스키마/데이터 정합성 (`appscript/Code.gs`)

| 항목 | 변경 |
|---|---|
| 신청관리 시트 헤더 마이그레이션 | `initSheets()`가 기존 시트의 누락 헤더(20·21번 `담당자`, `공정흐름도`)를 자동 보강. 빈 셀만 채우고 기존 헤더는 그대로 유지 |
| 신청관리 컬럼 20 (담당자) — ID → 이름 | `handleUpdateAssignee`가 `data.assigneeName` 우선 저장. admin.html `saveAssignee`도 함께 이름 전송 |
| 견적서발급관리 — 확정행 보호 | `handleConfirm`이 (접수번호+견적번호) 매칭 행이 이미 `confirmed`면 절대 갱신하지 않고 신규 행 추가 (이력 손실 방지) |
| 담당자관리 6컬럼화 | `직책` 컬럼 제거 → 이름 컬럼에 통합 (`박현석 부장`). `getUserConfig`/`handleSaveUserConfig` 인덱스 시프트 |
| PDF 보존 정책 | 파일명에 버전(`_v1.pdf`) 부착으로 자연스럽게 분리. 휴지통 이동 로직은 동일 버전 재발급에만 적용 |

### 12-B. 견적번호 형식 변경

`generateQuoteNo()` 산출물: `YYMMDD-NNNN` → **`YYMMDD-NNNN-v1`** (v1 접미사 부착).
`getNextVersion()`은 `-v1` → `-v2` → `-v3` ... 증분. 레거시 `-R\d+`도 다음 버전부터 `-v` 형식으로 자동 전환.

### 12-C. 담당자 = 시트 단일 진실 소스

- `DEFAULT_USERS` 하드코딩 배열 **삭제**. `getUsers()` 폴백은 빈 배열
- `u.title` 참조 **8군데 모두 제거** (`u.name + ' ' + u.title` → `u.name`)
- 신규 담당자 등록 폼: 이름란에 "이름 + 직책" 함께 입력하도록 안내
- 직책 컬럼이 이미 사용자에 의해 시트에서 삭제됨 → 코드 인덱스도 6컬럼으로 시프트

### 12-D. 버전 이력 UI 개선

- 1개 버전부터 표시 (이전엔 ≤1이면 숨김)
- 행 클릭 시 우측 패널이 그 버전 견적 상세로 갱신 (읽기 전용 + 액션바: 이 버전 출력 / 최신으로 돌아가기)
- 노란색 상단 배너 제거 → **단색 SVG 눈 아이콘** + 옅은 노란 배경(`#fef9c3`)으로 단순화
- 최신 버전 행 클릭 시엔 강조 X
- 변경정보 모달의 추가옵션: 이전/변경 후 두 셀로 단순 비교 (강조·삭선 제거)

### 12-E. 세션 자동 로그인

새로고침 시 로그아웃 방지 — `sessionStorage('jhtech_session_user')`에 사용자 ID만 저장.
탭 종료 시 자동 정리되어 공용 PC에서도 비교적 안전. 비밀번호는 저장하지 않음.

```
doLogin → sessionStorage.setItem → _enterApp
logout → sessionStorage.removeItem
initApp 끝 → tryRestoreSession (담당자 시트 로드 후 사용자 객체 복원)
```

### 12-F. 직인 설정 → 모달 탭

- 상단 GAS URL 노란 줄 제거 (hidden input만 호환용으로 남김. URL은 `config.js`로 관리)
- 직인 설정 라벨 헤더에서 제거 → 관리자 설정 모달 안 신규 **3번째 탭** `🔏 직인 설정`
- 좌측 미리보기 박스(220×220) + 우측 파일 업로드/삭제 버튼

### 12-G. PDF 생성 안정화

- **견적서 PDF**: 견적번호 옆 둥근 버전 라벨 제거 (견적번호 자체가 `-v1`이라 중복)
- **장비사진 PDF**: `inlineImages()`를 fetch → **XHR + responseType:'blob'**으로 전환. Chrome file:// CORS 제약 우회. 로컬 테스트는 `python3 -m http.server` 안내 (배포 환경 https://에서는 그대로 작동)
- **Drive 토스트 제거**: 우하단 초록 토스트의 링크가 마지막 업로드 파일(장비사진) URL만 가리켜 혼란 → 함수 자체 삭제

### 12-H. 신청 상세 화면 Enterprise CPQ 디자인 (변형 C)

해외 B2B SaaS(Stripe Invoicing, Salesforce CPQ, HubSpot Quotes, Pipedrive)의 디자인 패턴을 참고하여 우측 패널을 **좌우 2단 + sticky 우측 Quote Summary**로 재구성. 디자인 비교용 샘플은 `docs/layout-samples/` 폴더에 보관.

| 영역 | 변경 |
|---|---|
| 헤더 | `재현테크 logo-header.png` (280×63, jhtech.co.kr에서 가져옴) + "디지털 프린팅의 중심" 태그라인 + 세로 구분선 + "견적 관리" 역할 라벨 |
| 사이드바 | 카드형 항목(그림자, hover transform), pill 탭 그룹, 세로 막대 헤더 |
| 메인 그리드 | `1fr / 340px sticky` (1280px 미만 1단 폴백) |
| Deal banner | 회사명 + 상태 badge + KPI 4종 (견적번호 / 담당자 / 유효기간 / 합계금액 — `id="bannerTotal"`) |
| 신청기업 정보 | 3열 grid + 인라인 담당자(아바타) + 문제공정/도입목적 ctx-row + quote-issue 박스 (간격 14px) |
| 선택 장비 hero | 4:3 사진 + 카테고리 pill + 26px 모델명 + spec 테이블 + 카드 footer (편집 시 select / 보기 시 견적번호 pill) |
| 포함 옵션 | opt-row (옅은 회색 박스 + 파란 hover) |
| 추가 옵션 | chip 제안 + 인라인 입력 ex-tbl + 점선 "직접 추가" 버튼 |
| Quote Summary (우측) | 그라데이션 헤더 + 장비/옵션 섹션(itemized + **소계 박스**) + 노란 합계 박스(30px) + 액션 + 메타 |
| 영업일지 | 우측 sticky Quote Summary **아래**로 이동, 너비 동일, 노란 톤 → 차분한 화이트 톤 |

### 12-I. recalcPrice 강화 + 합계 미갱신 버그 동반 수정

```js
function recalcPrice(){
  // basePrice input 없는 view 모드에서도 currentReq에서 계산
  // 갱신 대상: totalDisplay, bannerTotal, equipSubtotal, extraSubtotal, extraCount,
  //          taxPrice, totalPrice, supplyPrice (hidden), extraPriceBreakdown
}
```
`basePrice` input의 oninput에 `recalcPrice()` 호출 추가 (이전엔 `formatPriceInput`만 호출되어 합계 미갱신).

### 12-J. 추가옵션 마지막 행 높이 통일

CSS `.price-row:last-child` 셀렉터가 `#extraPriceBreakdown` 내부 마지막 옵션 행에도 상속되어 합계 행 스타일이 잘못 적용되는 문제 → `.price-summary > .price-row:last-child`로 한정.

### 12-K. 견적금액 카드의 '저장' 버튼 제거

`syncToSheet`을 즉시 호출하는 부수효과 발생 → 임시저장/견적확정으로만 시트 기록되게 정리.

### 12-L. 신청중 첫 클릭 시 포함옵션 자동 체크

`selectReq()`에서 `status='new'` + `includeOpts`가 비어있을 때 장비 기본 includeOpts로 채움 (이전엔 장비 변경 시에만 자동 체크됐고 첫 진입은 모두 해제).

### 12-M. 첫 로그인 데이터 로딩 오버레이

빈 화면 대신 중앙 카드(회전 링 스피너 + "데이터 불러오는 중" + GAS 연결 라벨). 첫 로드(`!allRequests.length` + `!silent`)에만 표시. 폴링/visibilitychange 갱신엔 표시 안 함.

### 12-N. 로고 정리

- 기존 `logo.png` (800×800 정사각형) — OG 메타용. 유지
- 신규 `logo-header.png` (280×63 가로형) — admin.html 헤더 브랜드 영역 전용. jhtech.co.kr 공식 사이트의 `wp-content/uploads/2020/11/logo280.png`에서 가져옴

### 변경 라인 분량

| 파일 | 변경 |
|---|---|
| `admin.html` | 약 +1,300 / -340 줄 (헤더, 사이드바, renderDetail 전면 재작성, v2 CSS 약 300줄 추가) |
| `appscript/Code.gs` | 약 +60 / -30 줄 (시트 마이그레이션, 6컬럼화, 확정행 보호, 이름 저장) |
| `docs/issues.md` | F18~F26 + 문제 8~12 추가 누적 |
| `docs/code-analysis.md` | 경로 갱신 |
| `docs/layout-samples/` | 신규 — `index.html` + `variant-a-invoice.html` + `variant-b-minimal.html` + `variant-c-enterprise.html` + `_base.css` + `logo.png` |
| `logo-header.png` | 신규 |

### GAS 배포 (필수)

```bash
pbcopy < /Users/seonjecho/Projects/jhtechsmart/appscript/Code.gs
```
→ Apps Script 에디터 → 전체 붙여넣기 → 저장 → **`initSheets` 1회 실행**(시트 마이그레이션) → "배포 관리 → 기존 배포 편집 → 새 버전" 배포. URL은 변경되지 않음.

### 검증 체크리스트

- [x] 헤더 로고/태그라인 정상 표시 (logo-header.png 사용)
- [x] 사이드바 항목 카드 스타일 + hover 효과
- [x] 신청 클릭 → deal banner + sticky Quote Summary 즉시 표시
- [x] 가격 입력 → 장비 소계 / 옵션 소계 / 합계 / 상단 배너 실시간 동기화
- [x] 추가옵션 추가/삭제 → 우측 breakdown 동기화
- [x] 영업일지가 우측 Quote Summary 아래 위치
- [x] 견적 확정 시 PDF가 `견적서_업체명_v1.pdf` 형식으로 Drive 저장
- [x] 수정 확정 시 v2 PDF 추가 저장 (v1 보존)
- [x] 버전 이력 행 클릭 → 우측 패널이 그 버전 상세로 갱신
- [x] 첫 로그인 시 로딩 오버레이 표시 (~0.3~1.5초)
- [x] 새로고침해도 세션 유지 (탭 닫으면 자동 로그아웃)

---

## 13차 작업 — 토큰 기반 인증 + 백엔드 권한 검증 (B안)

**작업일**: 2026-05-12
**상태**: ⚠ **테스트 환경(별도 GAS 프로젝트 + 별도 시트)에서 검증 진행 중. 운영 적용 미완료. 커밋 보류.**
**관련 파일**: `admin.html`, `appscript/Code.gs`, `config.js`(테스트 URL로 임시 변경), `docs/auth-system.md`(신규)
**개발 노트**: 자세한 설명은 `docs/auth-system.md`(이 회차의 결정/구조/시나리오 정리) 참조

### 배경 — v12 이하의 본질적 한계

```
[브라우저] → fetch ?action=list → [GAS]
                                   │ 모든 행 반환
[브라우저] ← (전체 데이터) ────────┘
   │
   └ 클라이언트가 if(!isAdmin) filter — 화면에만 숨김
```

- GAS가 모든 신청 데이터를 무조건 반환 → F12 콘솔에서 다른 업체 사업자번호·연락처·이메일 모두 조회 가능
- 비밀번호가 모든 사용자에게 다운로드됨 (`getUserConfig` 응답에 pw 포함)
- 담당자 매핑이 localStorage 의존 → PC 종속

### v13 — 토큰 인증 + 권한 필터 도입

#### GAS 측 (`appscript/Code.gs`)

- `generateToken(user)` / `verifyToken(token)` — HMAC-SHA256 자가서명 토큰 (8시간 만료)
- `getSecret()` — Script Properties `SECRET` 자동 생성·관리
- `handleLogin(data)` — id/pw 검증 + 토큰 + 부트스트랩 데이터(rows) 반환
- `doPost/doGet` 모든 action 진입에 `verifyToken` 검증 (submit·login만 예외)
- `_filteredRequestRows(user)` — 비관리자는 `r.assignee === user.name`인 행만 반환
- `_checkRequestPermission(reqId, user)` — confirm/saveQuote 등 mutation에 권한 체크
- `handleUpdateAssignee` / `handleSaveEquipConfig` / `handleSaveUserConfig` — **관리자 전용**
- `_readUserConfigMasked()` — 비밀번호 마스킹 (`pw: ''`)
- `whoami` 엔드포인트 — 토큰 검증 + 페이로드 반환 (세션 복원용)

#### admin.html 측

- `gasCall(action, data, opts)` — sessionStorage 토큰 자동 첨부 + `AUTH_REQUIRED` 자동 처리
- `_setToken/_getToken/_clearToken` — sessionStorage `jhtech_session_token` 관리
- `handleAuthExpired` — 만료/위변조 응답 시 로그인 화면 자동 복귀
- `doLogin` GAS 호출로 전환 (id/pw 전송 → 토큰 + 부트스트랩 응답)
- `_enterApp(bootstrap)` — 토큰 + 사용자 + 신청 목록으로 화면 진입
- `tryRestoreSession` — 페이지 로드 시 토큰으로 자동 로그인
- `_clearScreen` — 로그아웃 시 메모리(currentUser, allRequests, _memUsers, _memEquipOpts) + DOM(mainArea, reqList) 완전 정리
- `getAssignees/saveAssignees` 및 `jhtech_assignees` localStorage 매핑 **완전 제거**
- `renderList` 단순화 — 서버 필터된 응답 그대로 사용
- `isAssignedToMe` — `r.assignee === currentUser.id` 비교

### v14 — 시트 컬럼을 ID로 전환 (한글 비교 이슈 해결)

#### 문제 (v13)

신청관리 컬럼 20 / 견적서발급관리 컬럼 13에 한글 이름 저장. 한글 비교의 미세한 차이(NFC/NFD 자모 분리, trailing space)로 권한 매칭 실패 발생.

#### 해결

| 측면 | v13 | v14 |
|---|---|---|
| 신청관리 컬럼 20 | `"김태스트 사원"` | `"test1"` |
| 견적서발급관리 컬럼 13 | `"김태스트 사원"` | `"test1"` |
| 권한 비교 | `r.assignee === user.name` | `r.assignee === user.id` |
| 화면 표시 | r.assignee 그대로 | `_users.find(u=>u.id===r.assignee).name` |
| 견적서 PDF 담당자 | name 매칭 | ID 매칭 후 .name 표시 |

#### 마이그레이션

`migrateAssigneeNameToId()` 함수 추가 — 신청관리/견적서발급관리 시트의 기존 이름을 ID로 자동 변환. 1회 실행. 멱등(이미 ID면 skip).

### v14 후속 패치 (테스트 중 발견·수정)

1. **로그아웃 후 화면 정리 보안 누락** — `_clearScreen` 헬퍼로 mainArea·reqList·메모리·sessionStorage 통째 정리. 이전 사용자 데이터 흔적 차단
2. **로그인 버튼 비활성화 버그** — `logout()` / `handleAuthExpired()`에서 `loginBtn.disabled = false` 명시
3. **헤더에 옛 이름 표시** — 토큰 페이로드는 발급 시점 스냅샷이라 시트 수정 후에도 옛 이름. `_loadAuxConfigInBackground`에서 _memUsers 기준으로 currentUser.name·phone 보정 + 헤더 자동 갱신
4. **다른 탭 갔다 와서 빈 화면** — silent 폴링이 빈 응답 받았을 때 이전 상태 유지. mainArea reset은 명시적 새로고침 시에만
5. **권한 거부 시 클라이언트 상태 롤백** — confirmQuote/saveDraft/confirmRevise에서 syncToSheet 실패 시 status 되돌리기 + PDF 생성 안 함
6. **printQuote 담당자 매칭** — `u.name === r.assignee` (남아있던 v13 코드) → `u.id === r.assignee` 일관 적용

### 로딩 속도 최적화

- handleLogin 응답에서 equipConfig/userConfig **제거** → token + user + rows만 (시트 호출 4회 → 2회)
- 클라이언트가 _enterApp 직후 백그라운드 fetch (사용자 화면 즉시 진입)
- equipConfig 캐시 (CacheService, 10분 TTL) — 변경 시 `handleSaveEquipConfig`에서 자동 무효화
- 단계별 로딩 메시지 ("인증 정보 확인 중…" → "신청 목록 불러오는 중…")

### SPREADSHEET_ID — Script Properties로 분기

```js
function _getSpreadsheetId(){
  const v = PropertiesService.getScriptProperties().getProperty('SPREADSHEET_ID');
  if(!v) throw new Error('Script Property "SPREADSHEET_ID"가 설정되지 않았습니다');
  return v;
}
```

코드에 시트 ID 박지 않음. 운영/테스트 GAS 각각 Script Property에 다른 ID 설정.

### config.js 임시 상태

- 활성: 테스트 GAS URL
- 주석: 운영 GAS URL
- 운영 배포 직전에 두 줄 토글 + 푸시 필요

### 보안 모델 (자세한 설명은 docs/auth-system.md)

#### ✅ 차단된 위협
- 비관리자의 다른 업체 데이터 조회 (서버 응답 자체가 권한 필터됨)
- URL 직접 호출 (토큰 없이는 AUTH_REQUIRED)
- 비밀번호 클라이언트 다운로드 (마스킹)
- 토큰 변조 (HMAC 서명)
- `currentUser.isAdmin = true` 메모리 변조 (서버는 토큰만 신뢰)

#### ⚠ 알려진 한계
- 비밀번호 평문 시트 저장 (단일 PC 신뢰 환경 전제)
- 토큰 즉시 무효화 불가 (8시간 유효)
- Refresh Token 미사용 (단일 토큰)
- Audit Log 없음
- Rate Limiting 없음

→ `docs/auth-system.md` Part 13에 보안 점수표 정리. 향후 개선 로드맵은 Part 14에 코드 예시 포함.

### 테스트 환경 / 운영 적용 절차

**테스트 환경 셋업 (1회)**:
1. 운영 시트 사본 생성 → 테스트 시트
2. GAS 프로젝트 사본 또는 신규 → Script Properties에 `SPREADSHEET_ID` = 테스트 시트 ID 설정
3. 테스트 GAS Web App 배포 → 별도 URL 발급
4. config.js에서 테스트 URL로 임시 변경 (커밋 X)
5. 로컬 `python3 -m http.server 8000`으로 admin.html 테스트

**운영 적용 (모든 테스트 통과 후)**:
1. 운영 시트 백업 사본 생성
2. 운영 GAS 프로젝트의 Script Properties에 `SPREADSHEET_ID` = 운영 시트 ID 추가
3. 운영 GAS 코드 전체 교체 + 저장
4. **운영 GAS 에디터에서 `migrateAssigneeNameToId` 1회 실행** (시트 이름→ID 변환)
5. 운영 GAS 새 버전 배포
6. config.js 운영 URL로 복원 (테스트 URL 주석 처리)
7. admin.html + config.js GitHub 푸시 → GitHub Pages 자동 반영
8. 운영 환경 즉시 로그인 테스트

### 변경 라인 분량

| 파일 | 변경 |
|---|---|
| `admin.html` | gasCall, _enterApp, _loadAuxConfigInBackground, _clearScreen, renderList, isAssignedToMe, saveAssignee, printQuote, syncToSheet, tryRestoreSession, doLogin, logout, handleAuthExpired 등 — 약 400줄 |
| `appscript/Code.gs` | 전체 재작성 (인증·토큰·권한·마이그레이션 + 캐시) — 약 600줄 |
| `docs/auth-system.md` | 신규 — 약 600줄 (개발 노트) |

### 외부 산출물 (jhtechsmart 리포지토리 외)

`/Users/seonjecho/Projects/Develop_Note/` 폴더에 사용자 관리 시스템 전반 가이드 별도 작성:
- `사용자관리_개발가이드_2026-05-12.md` (2,085줄)
- `사용자관리_개발가이드_2026-05-12.html` (1,107줄)

NIST/OWASP 표준 + Google·Netflix·Stripe·Auth0 실무 패턴 + 현재 프로젝트 진단 + 개선 로드맵 포함. 향후 사용자 관리 작업 시 표준 참고서.

### 검증 진행 상태 (테스트 GAS)

- [x] 토큰 기반 로그인 + sessionStorage 보관
- [x] 비관리자(test1) 본인 담당 신청만 표시 (서버 필터)
- [x] 권한 우회 시도 차단 (다른 사람 reqId mutation, 인증 없는 fetch)
- [x] 로그아웃 후 다른 사용자 로그인 시 이전 데이터 흔적 X
- [x] 세션 자동 복원 (Cmd+R, Cmd+Shift+R)
- [x] 시트 ID 분기 (Script Properties)
- [x] 견적서 PDF 담당자 표시 (ID→이름 변환)
- [ ] 8시간 토큰 만료 자동 처리 (수동 sessionStorage.removeItem으로 확인)
- [ ] 모든 시나리오 회귀 테스트 (D~I 섹션)
- [ ] 운영 시트 마이그레이션 dry-run

### 다음 단계

1. 테스트 환경에서 잔여 시나리오 검증
2. 검증 완료 후 운영 배포 (위 절차)
3. 운영 배포 직후 작업내역.md에 "운영 적용 완료" 메모 추가
4. (선택) bcrypt 비밀번호 해싱, Audit Log, Rate Limiting 등 후속 개선 — `docs/auth-system.md` Part 14 참조

### 미커밋 상태 (의도적)

이번 차수는 **운영 환경 라이브 검증 완료 후 커밋·푸시**할 예정. 현재 working tree 상태:

```
M admin.html
M appscript/Code.gs
M config.js              ← 테스트 URL로 변경됨, 운영 배포 직전 복원 필요
?? docs/auth-system.md
```

운영 적용 시 한 번에 커밋: `feat: 토큰 기반 인증 + 백엔드 권한 검증 (13차)`

---

## 14차 작업 — 견적서 버전이력 / 추가옵션 UI / 인감 배포

**작업일**: 2026-05-12
**관련 파일**: `admin.html`, `.gitignore`, `견적서/법인도장.png`(신규 커밋), `docs/issues.md`
**상태**: 13차 인증과 함께 운영 배포

### 버그 수정

| # | 증상 | 원인 | 수정 |
|---|---|---|---|
| 1 | 견적서 수정확정·확정 후 좌측 업체를 재클릭해야 버전이력이 화면에 반영됨 | confirmRevise/confirmQuote가 시트에는 새 행을 저장하지만 클라이언트 `_reqVersions` / `currentReq.versions`를 갱신하지 않음. 60초 폴링이 돌기 전까지 옛 데이터 | `refreshVersionsAfterPdf(reqId)` 헬퍼 추가. PDF 저장 800ms 뒤 `loadVersionsForReq` 호출 → `_reqVersions` 즉시 fetch + `renderVersionHistory` 자동 실행. allRequests 내 해당 신청.versions도 동기 |
| 2 | 버전이력 행 클릭 시 중앙 패널은 갱신되는데 우측 Quote Summary가 최신 버전 값으로 유지 | `renderDetail` 끝의 `recalcPrice()`가 viewingPastVersion 모드에서 basePrice input이 없을 때 `currentReq.supplyPrice`(=최신값)를 base로 잡아 `#totalDisplay`/`#equipSubtotal`/`#extraSubtotal`/`#extraPriceBreakdown`을 모두 덮어씀 | `if(!viewingPastVersion) recalcPrice();` 가드. 인라인 HTML이 이미 r(과거 버전 데이터) 기준 정확히 렌더되어 있으므로 호출 자체를 건너뛰면 됨 |
| 3 | 추가옵션 박스의 옵션명·단가 입력 필드가 흰 배경에 보더 없어 시각적으로 input임을 알기 어려움 | `.ex-tbl input` 룰이 `border:0;background:transparent`로 일괄 적용 — 편집/읽기 모드 구분 없음 | `.ex-tbl input:not(:disabled)`에 옅은 보더(#e2e8f0) + 흰 배경 + 라운드(7px) + padding. hover/focus 단계적 강조. placeholder 추가("옵션명을 입력하세요" / "0"). 테이블 자체에 옅은 배경(#fafbfd) |
| 4 | 신청중 상태(버전이력 없음) 업체에서 좌측 "신청기업 정보" 박스와 우측 "Quote Summary" 박스의 윗라인이 14px 어긋남 | `v2-left`가 flex(gap:14px)인데 빈 `<div id="versionHistorySection"></div>`가 첫 자식이라 빈 상태에서도 gap이 적용되어 좌측이 14px 밀려남 | `#versionHistorySection:empty{display:none}` — 빈 상태에서 flex item 카운트 제외 |

### 인감 이미지 배포 포함

기존: `견적서/법인도장.png`를 `.gitignore`로 제외(로컬 전용), `admin.html`의 `loadStamp()`이 localStorage `jhtech_stamp_b64` 또는 그 파일을 폴백으로 시도.

문제: 새 PC / GitHub Pages 환경에서는 localStorage 비어있고 PNG도 없어 견적서 PDF에 인감이 안 찍힘.

해결:
- `.gitignore`에서 `견적서/법인도장.png` 라인 주석 처리 (Public 노출 우려는 사용자가 인지 후 결정)
- 인감 PNG 파일을 `견적서/법인도장.png`에 배치 후 git add
- 다른 PC에서도 어드민이 자동으로 인감 로드 가능

### 변경 라인 분량

| 파일 | 변경 |
|---|---|
| `admin.html` | refreshVersionsAfterPdf 함수, viewingPastVersion 가드, .ex-tbl CSS 룰, versionHistorySection:empty, placeholder 추가 — 약 25줄 |
| `.gitignore` | 인감 라인 주석 — 1줄 |
| `견적서/법인도장.png` | 신규 트래킹 |
| `docs/issues.md` | 운영 적용 상태 갱신 |

### 검증 체크리스트

- [ ] 신청중 상태 업체 클릭 → 좌측 신청기업 정보 박스와 우측 Quote Summary 박스의 윗라인이 같은 위치
- [ ] 버전이력 있는 업체 클릭 → 좌측 버전이력 박스와 우측 Quote Summary 박스 윗라인 정렬 (기존과 동일)
- [ ] 견적 확정 직후 → 버전이력 행이 화면에 즉시 추가됨 (왼쪽 재클릭 불필요)
- [ ] 수정 확정(버전업) 직후 → 새 버전 행이 즉시 추가됨
- [ ] 과거 버전 행 클릭 → 우측 Quote Summary의 장비/옵션/합계 모두 그 버전 값으로 변경
- [ ] "최신 버전" 버튼 클릭 → 다시 최신 값으로 복귀
- [ ] 추가옵션 input에 보더+흰 배경 표시, hover/focus 강조, placeholder 보임
- [ ] 새 PC / 시크릿창에서도 견적서 PDF에 인감이 찍힘 (localStorage 비어 있어도 PNG 폴백 작동)

## 15차 작업 — 신청자격 기준 변경 (매출 2억원 미만이 지원 대상)

**작업일**: 2026-06-15
**관련 파일**: `quote.html`, `appscript/Code.gs`, `CLAUDE.md`
**상태**: quote.html 커밋 / Code.gs는 GAS 재배포 필요

### 배경
사업 공고 기준 변경으로 신청자격 방향이 **반전**됨.
- 구: 3년 평균 연매출 2억원 **이상** 또는 2025년 매출 2억원 이상이 지원 대상
- 신: **직전년도(2025) 매출 2억원 미만 또는 최근 3개년 연평균 매출 2억원 미만**이 지원 대상

### 변경 내용

| 파일 | 위치 | 변경 |
|---|---|---|
| `quote.html` | 사업 핵심 요약 신청자격 | 문구를 새 조건으로 교체 |
| `quote.html` | `calcRevenue()` | 판정식 `ok = (rev2025 < limit) \|\| (avg < limit)` — 둘 다 2억 이상일 때만 제외. 결과 메시지에 2025·평균 두 조건 각각 표시 |
| `quote.html` | 주업종 드롭다운 안내문구 | "지원 기준: 평균매출 2억원 이상" → "지원 기준: 매출 2억원 미만" (option value 불변) |
| `appscript/Code.gs` | GPT 프롬프트 예시 5곳 | 동일 문구 "매출 2억원 미만"으로 동기화 (잡음 제거 지침 + fewshot 입력 샘플) |
| `CLAUDE.md` | 7번 항목 | 매출 판정 기준 반전 반영 |

### 검증
- 새 로직 4케이스: 둘 다 ≥2억 → 제외 / 그 외(하나라도 <2억) → 가능 — 정상
- 인라인 스크립트 구문 검사 통과 / 브라우저 실측 결과 정상

### ⚠️ 5차 작업 대비 방향 반전
5차에서 `ok = avg >= limit`(2억 이상 자격)로 잡았던 부등호가 이번에 정반대로 바뀜. 이후 `calcRevenue` 관련 작업은 **최신 기준(2억 미만 자격, OR 조건)** 으로 볼 것.
